name: Deploy e-commerce-api
on:
  push:
    branches: 
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4
      
      - name: Login to docker hub
        run: docker login -u ${{secrets.DOCKER_USERNAME}} -p ${{secrets.DOCKER_PASSWORD}}
      
      - name: Build Docker Image
        run: docker build -t  bayrambayraktar/ ^^^^ .

      - name: Publis Image to docker hub
        run: docker push bayrambayraktar/^^^^:latest

    deploy: 
      needs: build
      runs-on: self-hosted
      steps:
        - name: Pull image from docker hub
          run: docker pull bayrambayraktar/e-commerce-backend:latest

        - name: Delete old container
          run: docker rm -f e-commerce-backend-container

        - name: Run Docker Container
          run: docker run -d -p 5000:5000 --name e-commerce-backend-container -e MONGO_PASSWORD='${{secrets.MONGO_PASSWORD}}' bayrambayraktar/e-commerce-backend
        